# port where hello-cloud-service will run
server.port=7979

# allow for RefreshScope
management.endpoints.web.exposure.include=*

# the hello-sloud-service will use this as the greeting that it serves up
officialGreeting="This is the new greeting reset with Curl Command!!!"

# locate and call the Quote Service
quoteServiceName=quote-service
serviceProtocol=http://
servicePath=/quote
